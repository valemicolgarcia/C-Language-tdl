#include <stdio.h>
#include <stdlib.h>


    int main (int argc, char * argv[]) {
        printf("\nargc = %d",argc); //numero de argumentos pasados, ruta completa al programa que se ejecuta,
        printf("\nargv[0] => %s",argv[0]); // vector de cadenas de caracteres
    return 0;
    }
